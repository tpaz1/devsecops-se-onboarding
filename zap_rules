# zap-api-scan rule configuration file
# Change WARN to IGNORE to ignore rule or FAIL to fail if rule matches
# Active scan rules set to IGNORE will not be run which will speed up the scan
# Only the rule identifiers are used - the names are just for info
# You can add your own messages to each rule by appending them after a tab on each line.

# Unexpected Content-Type was returned
100001	IGNORE	Ignore Unexpected Content-Type

# Client/Server error response codes (400/404/500 etc.)
100000	IGNORE	Ignore HTTP Error Codes

# ZAP is Out of Date (Medium)
10116	IGNORE	Ignore ZAP Version Check

# Non-Storable Content
10049	IGNORE	Ignore Non-Storable Content

# Sec-Fetch-* headers missing
90005	IGNORE	Ignore Sec-Fetch Headers

# User Agent Fuzzer
10104	IGNORE	Ignore User-Agent Fuzzer
