# zap-api-scan rule configuration file
# Change WARN to IGNORE to ignore rule or FAIL to fail if rule matches
# Active scan rules set to IGNORE will not be run which will speed up the scan

# Unexpected Content-Type was returned
100001 IGNORE

# Client/Server error response codes (400/404/500 etc.)
100000 IGNORE

# ZAP is Out of Date (Medium)
10116 IGNORE

# Non-Storable Content
10049 IGNORE

# Sec-Fetch-* headers missing (all 4 are the same plugin id)
90005 IGNORE

# User Agent Fuzzer
10104 IGNORE
